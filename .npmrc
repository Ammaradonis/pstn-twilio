engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# Flat node_modules layout — required for the API's webpack externals to resolve
# transitive deps (express, etc.) at runtime in production.
node-linker=hoisted
